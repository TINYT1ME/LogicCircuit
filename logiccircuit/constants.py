# Constants that do not depend on pygame

WIN_NAME = "Logic Gates and what not"  # window name
FPS = 60  # FPS
WIDTH, HEIGHT = 900, 500  # window size
BASIC_SIZE = (int(WIDTH * 0.0277 * 2), int(WIDTH * 0.0277))  # default size for button
